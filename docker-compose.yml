services:
  meilisearch:
    image: getmeili/meilisearch:v1.40
    container_name: meilisearch_1
    restart: unless-stopped
    ports:
      - "7700:7700"
    env_file:
      - .env
    volumes:
      - ./meilisearch_data:/meili_data